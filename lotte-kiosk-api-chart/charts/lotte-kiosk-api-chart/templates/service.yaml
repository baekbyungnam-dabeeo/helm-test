apiVersion: v1
kind: Service
metadata:
  name: {{ include "lotte-kiosk-api-chart.fullname" . }}
  labels:
    {{- include "lotte-kiosk-api-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lotte-kiosk-api-chart.selectorLabels" . | nindent 4 }}
